export enum UserType {
  ADMIN = 'A',
  TEACHER = 'T',
  STUDENT = 'S',
}

export enum UserStatus {
  ACTIVE = 'A',
  PENDING = 'P',
  DISABLED = 'D',
}

export enum Gender {
  MALE = 'M',
  FEMALE = 'F',
  OTHER = 'O',
}

export enum StudentStatus {
  ACTIVE = 'A',
  PASSED = 'P',
  SUSPENDED = 'S',
}
